name: Temporary Av32 transition update
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/temp-av32-transition.yml'
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Av32
        shell: bash
        run: |
          echo "This workflow was disabled after a malformed-run failure. Av32 was not applied."
